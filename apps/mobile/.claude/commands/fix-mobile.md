Fix this mobile app issue: $ARGUMENTS

Steps:
1. Identify the relevant screen(s) in src/screens/
2. Check the corresponding API service in src/api/
3. Check the Zustand store if state-related
4. Check types/index.ts for type mismatches
5. Fix the issue
6. Run `npx tsc --noEmit` to verify no type errors
